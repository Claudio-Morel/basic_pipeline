- name: Desplegar servidor con FastAPI
  hosts: web
  become: true

  vars:
    app_path: /var/www/api
    venv_path: /var/www/api/venv

  tasks:

    - name: Instalar dependencias del sistema
      apt:
        name:
          - python3
          - python3-pip
          - python3-venv
        state: present
        update_cache: yes

    - name: Crear carpeta de la aplicación
      file:
        path: "{{ app_path }}"
        state: directory

    - name: Copiar aplicación FastAPI al servidor
      copy:
        src: ../app/
        dest: "{{ app_path }}"
        owner: root
        group: root
        mode: '0755'

    - name: Crear entorno virtual para FastAPI
      command: python3 -m venv {{ venv_path }}
      args:
        creates: "{{ venv_path }}/bin/activate"

    - name: Instalar dependencias dentro del entorno virtual
      command: "{{ venv_path }}/bin/pip install -r {{ app_path }}/requirements.txt"

    - name: Crear archivo de servicio systemd para FastAPI
      copy:
        dest: /etc/systemd/system/fastapi.service
        content: |
          [Unit]
          Description=FastAPI Application
          After=network.target

          [Service]
          User=root
          WorkingDirectory={{ app_path }}
          ExecStart={{ venv_path }}/bin/uvicorn app:app --host 0.0.0.0 --port 8000
          Restart=always

          [Install]
          WantedBy=multi-user.target

    - name: Recargar systemd
      systemd:
        daemon_reload: true

    - name: Iniciar servicio FastAPI
      systemd:
        name: fastapi
        state: restarted
        enabled: true
